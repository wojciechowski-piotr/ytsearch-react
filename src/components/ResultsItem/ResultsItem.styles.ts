import styled from 'styled-components';

export const Wrapper = styled.div`
    margin-bottom: 1rem;
    .card-root {
        display: flex;
        flex-flow: row nowrap;
        height: 150px;
        img {
            width: 200px;
            object-fit: cover;
        }
    }
`