import React from 'react';

import ResultsList from '../ResultsList';

import { Wrapper } from '../../App.styles';

const Home = () => {
    return (
        <Wrapper fixed>
            <ResultsList />
        </Wrapper>
    );
};

export default Home;
